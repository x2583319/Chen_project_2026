# Demo / sample data

This folder is reserved for lightweight demo or sample event files used by the QC workflow.

For the current prototype, full OpenNeuro imaging data are not stored here. The workflow uses remote AWS S3 listings for file-inventory smoke tests, and selected lightweight `events.tsv` files are saved as small examples.
